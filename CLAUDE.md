# Git commits

Never add a `Co-Authored-By: Claude ...` trailer, or any other AI-attribution line, to a
commit message in this repository — including the one the Bash tool's own default
instructions suggest. Commits are authored and committed solely as the repo owner
(`Mikkel Eskildsen <mke@visotia.com>`, matching the local git config). This applies in
every worktree of this repo, not just the primary checkout.
